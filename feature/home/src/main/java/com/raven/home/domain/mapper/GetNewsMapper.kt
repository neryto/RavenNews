package com.raven.home.domain.mapper

import javax.inject.Inject

class GetNewsMapper @Inject constructor() {

    suspend fun transformDomainToUI(params: List<Unit>)  {
        TODO("Transform your domain objects into view")
    }
}
